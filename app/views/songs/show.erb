<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>

    <% if flash.has?(:message) %>
  <%= flash[:message] %>
<% end %>

    <h3><%= @song.name %></h3>
    <a href="/artists/<%= @song.artist.slug %>"><h4><%= @song.artist.name %></h4></a>
    <p>Song's genre/s:</p>
    <% @song.genres.each do |genre| %>
      <a href="/genres/<%= genre.slug %>"><h5><%= genre.name %></h5></a>
    <% end %>
  </body>
</html>
